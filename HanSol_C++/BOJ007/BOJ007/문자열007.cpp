//백준 2908번 상수
	/*
#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int main()
{
	string A, B; //수 2개
	cin >> A >> B; //상근 수

	string A_r(A.rbegin(), A.rend()); //A 뒤집기
	//cout << stoi(A_r) << "\n";
	//cout << "자료형: " << typeid(stoi(A_r)).name() << "\n";
	string B_r(B.rbegin(), B.rend()); //B 뒤집기

	if (stoi(A_r) > stoi(B_r))
	{
		cout << A_r;
	}
	else if (stoi(A_r) < stoi(B_r))
	{
		cout << B_r;
	}

	return 0;
}
*/