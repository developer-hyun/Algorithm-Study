//백준 10809번 알파벳 찾기(26)
	/*
#include <iostream>
#include <string>

using namespace std;

int main()
{
	string s;
	cin >> s;
	string abc = "abcdefghijklmnopqrstuvwxyz";

	for (int i = 0; i < abc.length(); i++)
	{
		cout << (int)s.find(abc[i]) << " ";
		//str.find(문자열)
		//문자열 s와 abc를 비교해서 원하는 문자가 있을 때 해당 위치 반환
		//없으면 -1을 리턴
	}

	return 0;
}
*/